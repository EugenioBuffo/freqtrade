# Include all requirements to run the bot.
-r requirements.txt

plotly==5.14.1

# Telegram Graph Generation
kaleido~=0.2.1; platform_system != "Windows"
kaleido~=0.1.0post1; platform_system == "Windows"
